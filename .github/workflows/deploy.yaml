name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t raquelnatale/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true raquelnatale/react-test npm test

      - run: docker build -t raquelnatale/multi-client ./client
      - run: docker build -t raquelnatale/multi-nginx ./nginx
      - run: docker build -t raquelnatale/multi-server ./server
      - run: docker build -t raquelnatale/multi-worker ./worker

      - run: docker push raquelnatale/multi-client
      - run: docker push raquelnatale/multi-nginx
      - run: docker push raquelnatale/multi-server
      - run: docker push raquelnatale/multi-worker
